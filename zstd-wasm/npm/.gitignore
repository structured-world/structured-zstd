# Dependencies
node_modules/

# wasm-pack build outputs (regenerated by `npm run build:wasm:*` / CI / publish)
simd/
scalar/

# TypeScript build outputs (regenerated by `tsc`)
index.js
index.js.map
index.d.ts
index.d.ts.map

# Bundled LICENSE is copied from the repo root at build/publish time
LICENSE
